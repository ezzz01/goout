<% tag_cloud @tags, %w(css1 css2 css3 css4) do |tag, css_class| %>
  <%= link_to tag.name, @user.nil? ? zeitgeist_path(tag.name) : user_tag_posts_url(@user, tag.name), :class => css_class %>
<% end %>
