<table>
  <tr>
    <th><%= @country.title %></th>
  </tr>

<% @country.universities.each do |university| %>
  <tr>
    <td><%=h university.title %></td>
    <td><%= link_to 'Show', university %></td>
    <td><%= link_to 'Edit', edit_country_path(university) %></td>
    <td><%= link_to 'Destroy', university , :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>



<%= link_to 'Edit', edit_country_path(@country) %> |
<%= link_to 'Back', countries_path %>
