<% form_for :spec do |f| %>
  <fieldset>
    <legend><%= @title %></legend>
    <%= error_messages_for 'spec' %>
    <%= collection_select(:university, :id , @universities, :id, :title) %>
    <%= submit_tag "Update", :class => "submit" %>
 </fieldset>
<% end %>
