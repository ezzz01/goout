<h1>Listing studies</h1>

<table>
<% @studies.each do |study| %>
  <tr>
    <td><%=h study.university.title %></td> 
    <td><%=h study.from %></td> 
    <td><%=h study.to %></td> 
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New study', new_study_path %>
